# This file is automatically used by docker-compose for local development overrides
# Add your local environment variables here

services:
  billing-service:
    environment:
      # Override with your actual Stripe keys for local development
      # Never commit real keys to git!
      - Stripe__PublishableKey=pk_test_your_development_key_here
      - Stripe__SecretKey=sk_test_your_development_secret_here
